server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gycrm?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456

my:
  server:
  	address: http://localhost:${server.port}
  file:
  	location: /tmp/upload
  cors:
    allowed: true
    origin: '*'